# KL ERP Buddy backend — pinned dependencies.
# Direct deps + their hard runtime deps, pinned to the exact versions verified
# in the dev venv (pip freeze). All binary packages publish cp312 wheels, so
# this installs cleanly on the python:3.12-slim Docker image.

# --- Direct dependencies ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx[http2]==0.28.1
onnxruntime==1.28.0
numpy==2.5.1
pillow==12.3.0
beautifulsoup4==4.15.0
python-multipart==0.0.32

# --- Runtime dependencies of the above (from pip freeze) ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
colorama==0.4.6; sys_platform == "win32"
flatbuffers==25.12.19
h11==0.16.0
h2==4.4.0
hpack==4.2.0
httpcore==1.0.9
httptools==0.8.0
hyperframe==6.1.0
idna==3.18
packaging==26.2
protobuf==7.35.1
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
soupsieve==2.9.1
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvloop==0.22.1; sys_platform != "win32"
watchfiles==1.2.0
websockets==17.0.1
